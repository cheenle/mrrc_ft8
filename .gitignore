# Python
venv/
__pycache__/
*.pyc
.pytest_cache/
mrrc_ft8.egg-info/

# Build
dsp/build/
build/

# WSJT-X 3.0.2 Improved vendor source: kept on disk for DSP builds and the
# vendor-policy tests, but not tracked (98 MB third-party tree, v0.1 tidy).
wsjtx-3.0.2/

# Runtime data
mrrc-ft8.db
*.adi
*.wav
*.log
config.local.toml
data/

# Secrets
.env

# OS
.DS_Store

# Local design companion
.superpowers/
artifacts/dsp-benchmark.json

# Accidental recursive self-copy inside the skills dir (6+ GB of nested duplicates)
.agents/skills/.agents/

# Desktop client reference clone (pristine; do not commit)
# Anchored to repo root: unanchored "FT8web/" would also ignore desktop/ft8web
# on case-insensitive filesystems (git core.ignorecase).
/FT8web/
# Desktop client build artifacts
desktop/ft8web/node_modules/
desktop/ft8web/dist/
